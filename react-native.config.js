module.exports = {
    project: {
        ios: {},
        android: {}
    },
    assets: ['./assets/fonts']

    /**
     * Permite el uso de fuentes personalizadas a partir de la ruta especificada.
     * En caso de no reconocer la fuente especificada en algun componente, ejecutar el comando `npx react-native-asset` 
     * en la carpeta raiz del proyecto para hacer el enlace. Compilar nuevamente de ser necesario.
     */

}